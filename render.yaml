services:
  - type: web
    name: dropbeam-signaling
    runtime: node
    rootDir: signaling
    buildCommand: npm install
    startCommand: node index.js
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
